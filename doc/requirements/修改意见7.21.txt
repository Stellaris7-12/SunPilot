上午：
最好能完成业务闭环，讲好整个故事，会比较亮眼

把发单侧也做了，就是通话完成后（立刻分析通话语音进行识别和信息提取）-生成工单-处理工单-处理客户诉求-生成回单

对于通话数据，可以先构造通话记录数据集（文本），然后考虑用文本转语音（如果做语音通话识别）

PageAgent的功能：承接前面的多Agent处理后的数据，识别当前页面的元素，找到填单的位置，操作相应按钮、选项、以及文本框填写


总结一下：
“客服电话结束后发单Agent发单 -> PageAgent 辅助填单 ----解耦----- 回单Agent（多 Agent ）处理 -> PageAgent 辅助填单/回单 -> 结案复核”  让Pageagent贯穿全流程


下午：
调整Page Agent为第一优先级，要求的效果是像Ali的Pageagent一样，有炫酷的鼠标，我们能看到动态的点击过程，右边是Agent对话框，我们可以提要求，也可以看到Agent执行点击的过程信息（如果做成完全全自动处理的，那演示性会很弱）
添加发单Agent（用于识别通话记录和提取关键信息生成工单） 与原有回单侧Agent（多Agent）形成业务闭环

答辩时的目标不是像简历3个月的项目那样做一个无懈可击的系统或者产品（强稳定性，能应对故障等），而是展现自己对问题的思考，调研方案，选择方案，最后的效果。可以存在漏洞和待完善项，但是你要清楚项目目前存在的待优化点有哪些，如果要优化该如何去优化

星期四（7.23），需要完成所有开发，Demo完全准备好，并准备简单的演示PPT

待办：
1、Pageagent大规模引入
2、发单侧Agent开发（与回单侧是解耦的）
3、前端展示逻辑优化，显示和输出更平滑


发单Agent / 回单侧多Agent = 业务大脑
PageAgent = 页面执行员 + 可视化操作员
Mock Tools / MySQL = 业务系统与证据底座
人工坐席/处理员 = 最终责任人

通话记录
  -> 发单Agent：提取业务字段、生成工单草稿
  -> PageAgent：打开/识别发单页面，移动鼠标，自动回填字段，一键提交
  -> 多Agent：分类、抽取、工具调用、风险判断、生成回单
  -> PageAgent：打开工单处理页，定位证据，回填处理意见和客户回单，执行高频结案动作
  -> 人工/系统门禁：高风险确认、异常接管、最终审计