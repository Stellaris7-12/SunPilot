# 答辩PPT大纲 · 视觉版
**项目：** SunPilot — 基于多Agent的信用卡智能工单处理引擎  
**规格：** 11页（正文P04–P10 = 7页）· 5分钟汇报  
**主色：** 深红 `#C62B3B` · 辅色 深灰 `#2D2D2D` · 背景 `#FAFAFA`  
**字体：** 标题 思源黑体 Bold · 正文 思源黑体 Regular

---

## P01 · 标题页
📐 **版式：上下三段式（顶部装饰带 / 中部主标题区 / 底部信息栏）**

```
┌──────────────────────────────────────────────┐
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │  ← 深红色顶部装饰带（占页高15%）
│  [细线5-Agent流程图，白色半透明，横向居中]     │
├──────────────────────────────────────────────┤
│                                              │
│               SunPilot                       │  ← 主标题，64pt，深红
│                                              │
│     基于多Agent协同的信用卡工单智能处理引擎     │  ← 副标题，24pt，深灰
│                                              │
│    多Agent编排 · 双通道执行 · 人机协同         │  ← 关键词，14pt，灰色
│                                              │
├──────────────────────────────────────────────┤
│  汇报人：何云辉  │ 指导老师：___  │ 2026年7月 │  ← 底部信息栏，深红底白字
└──────────────────────────────────────────────┘
```

- 顶部装饰带：深红背景，内嵌5-Agent细线流程图（装饰用，半透明白色）
- 中部主标题区：白色背景，三行垂直居中
- 底部信息栏：深红背景白字，三项用竖线分隔，高度约占页高12%

---

## P02 · 个人介绍
📐 **版式：左1/4信息列 + 右3/4 内容区（上：教育/技能 / 下：核心贡献）**

```
┌────────┬─────────────────────────────────────┐
│  基本  │  教育背景                            │
│  信息  │  • 香港中文大学（深圳）计算机科学 硕士│
│        │    2025.9–2027.7                    │
│  姓名  │  • 北京理工大学 信息管理与信息系统    │
│  何云辉│    本科 2021.9–2025.6               │
│        ├─────────────────────────────────────┤
│        │  专业技能                            │
│        │  Java · Python · 数据库 · SpringBoot│
│        ├─────────────────────────────────────┤
│        │  荣誉奖项                            │
│        │  • SAE数据库全国科技信息检索大赛特等奖│
│        │  • 本科生奖学金二等奖                │
│        │  • CET-6 622 / 雅思 7.0             │
│        ├─────────────────────────────────────┤
│        │  科研/实践                           │
│        │  • 混合量子优化算法研究（项目成员）   │
│        │  • 青年志愿者协会 · 院合唱团         │
└────────┴─────────────────────────────────────┘
        
本项目核心贡献：
① 还原工单处理系统，搭建端到端处理管道
② 5Agent流水线 + Orchestrator设计与实现
③ 双通道执行架构设计 + PageAgent集成
④ SunPilot前端实时流光处理面板开发
```

- 左侧基本信息栏：浅灰底，姓名大字显示
- 右上方分4个区块：教育/技能/荣誉/科研实践，用细线分隔
- 底部核心贡献：4条用序号圆圈（红底白字）标注，独立区域突出显示

---

## P03 · 目录
📐 **版式：7个色块卡片，2行排列（4+3），每块圆角矩形**

```
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│ 01   │ │ 02   │ │ 03   │ │ 04   │
│业务  │ │方案  │ │系统  │ │典型  │
│背景  │ │选型  │ │架构  │ │场景  │
└──────┘ └──────┘ └──────┘ └──────┘
      ┌──────┐ ┌──────┐ ┌──────┐
      │ 05   │ │ 06   │ │ 07   │
      │功能  │ │创新  │ │个人  │
      │演示  │ │分析  │ │总结  │
      └──────┘ └──────┘ └──────┘
```

- 当前章节（若演示时手动高亮）用深红底白字；其余灰底深字
- 每块仅：编号（大） + 章节名（中）
- 7个章节对应：业务背景与痛点 / 方案调研与选型 / 系统架构设计 / 典型场景剖析 / 系统功能演示 / 创新点与总结 / 个人总结与展望

---

## P04 · 业务背景与痛点
📐 **版式：顶部3大数字横幅 + 下方痛点→方案对应表**

```
┌──────────────────────────────────────────┐
│   330万+        700+          24h+        │
│  年处理工单量   业务场景数    平均处理时长  │
└──────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  业务痛点                    →   解决措施                │
├─────────────────────────────────────────────────────────┤
│  工单字段繁多、结构多变       →  配置驱动 + 确定性优先    │
│  人工提取慢                       规则先行、LLM兜底      │
├─────────────────────────────────────────────────────────┤
│  内外系统割裂                 →  双通道执行架构          │
│  无统一处理入口                   同一决策大脑服务两路径  │
├─────────────────────────────────────────────────────────┤
│  外部系统无接口               →  PageAgent代填外部页面   │
│  靠人工重复填写                   坐席确认后提交          │
├─────────────────────────────────────────────────────────┤
│  金融场景要求全链路可追溯     →  5个独立Agent节点留痕    │
│  不可乱关单                       闭环必须人工确认        │
└─────────────────────────────────────────────────────────┘
```

- 3个大数字：字号60pt+，深红色，下方说明12pt灰色
- 痛点→方案表：左右箭头连接，深红箭头，视觉引导明确
- 底部小字标注："痛点决定了系统必须是「多Agent决策 + 双通道执行 + 全程可审计」"

> 🎤 话术：每年330万件工单，700+场景，平均处理超24小时。四个痛点一一对应四个解决措施，这不是技术偏好，是业务约束倒逼的架构决策。

---

## P05 · 方案调研与选型
📐 **版式：三行卡片，每行一个关键选型，左侧问题+右侧答案**

```
┌────────────────────────────────────────────────────────┐
│  ① 编排框架：手写 PipelineContext                       │
│                                                        │
│  为什么不用 LangGraph / AutoGen？                       │
│  ─────────────────────────────────────────────────── │
│  • 工单处理是确定性线性DAG，不需要图路由和多Agent协商    │
│  • 金融合规要求状态转换完全可预测、可审计               │
│  • 约百行代码清晰表达整个流程，零框架依赖               │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  ② 页面操作：场景感知的双范式                           │
│                                                        │
│  范式           定位依据              用在哪            │
│  ───────────────────────────────────────────────────  │
│  DOM索引+ReAct  DOM节点位置           外部系统          │
│                LLM动态分析            (无API、无改造权)  │
│  ───────────────────────────────────────────────────  │
│  语义化标签     稳定业务语义ID        自有Vue前端       │
│                对布局变化免疫         (拥有开发权)       │
│                                                        │
│  基于成熟开源浏览器操作引擎（MIT协议）二次开发           │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  ③ 决策策略：确定性优先 + LLM兜底                       │
│                                                        │
│  规则/schema先提取 → 字段全命中走快车道（1次LLM，~3秒） │
│  缺失时LLM只填空缺、不覆盖已提取值                       │
│  渐进式确定性化：新场景先走LLM，积累样本后固化为规则     │
└────────────────────────────────────────────────────────┘
```

- 三个卡片竖向排列，圆角白底，深红左边框4px
- 每卡顶部大标题（序号+名称），下方说明/对比表，简洁清晰
- 第二个卡片中的表格用细线分隔，避免过度装饰

> 🎤 话术：三个关键选型都不是技术偏好，是业务约束倒逼的结论。手写编排因为流程是线性的；双范式因为内外系统权限不同；确定性优先因为要控制成本和准确性。

---

## P06 · 系统架构设计（五层架构图）
📐 **版式：纵向五层堆叠，每层一个色块，图标+文字**

```
┌────────────────────────────────────────────────────────┐
│  ① 接入层                                               │
│  [图标] SunPilot前端实时流光面板 · PageAgent浏览器扩展  │
│         SSE流式输出                                     │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│  ② 编排层                                               │
│  [图标] Orchestrator 编排器 · PipelineContext 共享上下文│
│         高风险短路 / 风险门控 / 人工确认停顿点           │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│  ③ Agent层（5个业务Agent，均继承BaseAgent + AgentCard）│
│  [流程图] Classifier → Intake → Escalation →           │
│           Resolution → Notification                    │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│  ④ 执行层（双通道）                                     │
│  ┌─────────────────────┬─────────────────────┐         │
│  │ 通道一：内部系统     │ 通道二：外部系统     │         │
│  │ API直连             │ PageAgent代填        │         │
│  │ Mock Tools审计      │ 确定性语义执行为主   │         │
│  │ tool_registry校验   │ ReAct兜底           │         │
│  │                     │ 坐席确认后提交       │         │
│  └─────────────────────┴─────────────────────┘         │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│  ⑤ 数据与配置层                                         │
│  [图标] workflow_config.json（单一事实来源）           │
│         TDSQL持久化 · 两套LLM配置（业务DeepSeek /      │
│         PageAgent Qwen）                               │
└────────────────────────────────────────────────────────┘
```

- 五层用不同浅色底色区分（由上至下：浅蓝/浅绿/浅黄/浅橙/浅紫）
- 每层左侧序号圆圈（深红底白字），右侧图标+文字说明
- 第四层（执行层）特别强调：两个通道左右并列，用虚线分隔
- 底部强调文字："5Agent决策大脑在两条轨道上完全复用，配置是单一事实来源"

> 🎤 话术：五层架构，从接入到数据。重点看第三层和第四层：5个Agent构成决策大脑，下面两条执行通道复用同一套大脑，业务逻辑不重复。

---

## P07 · 典型场景剖析——伪冒调查全流程
📐 **版式：纵向流程图，左侧8步流程+右侧通道标注**

```
┌─────────────────────────────────────────────────────┐
│  场景：客户来电称卡片被系统管制、两核身不过          │
│  属于高风险伪冒调查场景，需查证卡片管制状态并发单    │
│  至调查组。端到端全流程：                            │
│                                                     │
│  ① 录音转标准工单 → 通话文本进入处理管道             │
│  ② Classifier 识别意图，命中「伪冒调查」场景         │
│     关键词：卡片被管制 / 两核身 / X-DK / BLOCK      │
│  ③ Intake 确定性提取字段（客户号、卡片列表、         │
│     管制原因、X-DK编号等）                          │
│     缺失字段交LLM补全，不覆盖已提取值                │
│  ④ Escalation 风险评估——伪冒调查为高风险场景，      │
│     命中风险门控，强制转人工确认                     │
│  ⑤ Resolution 规划工具调用，tool_registry 校验后    │
│     执行卡账户状态查询等                            │
│                                                     │
│  ⑥ 执行分通道：                                      │
│     ┌─────────────────┬─────────────────┐           │
│     │ 通道一（内部）  │ 通道二（外部）  │           │
│     │ 调用 card.     │ PageAgent打开    │           │
│     │ account-status │ 调查组发单页面   │           │
│     │ -query 查卡片  │ 语义填写工单     │           │
│     │ 管制状态       │ 坐席确认后提交   │           │
│     │ 结果写回工单   │                 │           │
│     │ 约3秒           │ 约10秒           │           │
│     └─────────────────┴─────────────────┘           │
│                                                     │
│  ⑦ Notification 生成回单建议，输出                   │
│     closureSuggestion.canClose（系统不自动关单）     │
│  ⑧ 人工审核关闭：坐席核验管制原因与调查组处理意见后  │
│     POST /close，闭环完成                           │
│                                                     │
│  ───────────────────────────────────────────────── │
│  时效对比：原人工流程 24小时+ → 系统处理 3–10秒      │
└─────────────────────────────────────────────────────┘
```

- 8步流程用序号圆圈（深红底白字）标注
- 第⑥步特别展开：双通道左右对比框，底色微区分
- 底部时效对比用大字深红标注，"24小时+"用删除线，箭头指向"3–10秒"
- 场景描述突出"高风险"关键词，用深红标注

> 🎤 话术：伪冒调查是高风险场景。从录音到关单，8个步骤。关键看第④步和第⑥步——第④步风险评估命中高风险门控，强制转人工确认；第⑥步同一套决策，两条执行路径。通道一查询卡片管制状态，通道二代填调查组发单页面需要坐席确认。最后系统不能自动关单，必须人工审核后才闭环。

---

## P08 · 系统功能演示（Demo）
📐 **版式：大视频区（中央）+ 下方三个功能亮点标签**

```
┌────────────────────────────────────────────┐
│                                            │
│           [ 视频演示区                      │
│             1280×720                       │
│             SunPilot 实时处理界面 ]          │
│                                            │
│                                            │
│      演示亮点：                             │
│      • 实时SSE流式输出，处理过程逐步可见     │
│      • SunPilot流光实时处理面板，Agent时间线│
│      • 双通道执行效果对比：API vs PageAgent │
│      • 缺失字段有界LLM补全后正常结案         │
│                                            │
└────────────────────────────────────────────┘
```

- 视频区占页面70%高度，深灰虚线边框占位
- 下方四个演示亮点用项目符号列出，不用过度装饰
- 顶部页眉小字："系统实际运行录屏展示"

---

## P09 · 创新点与总结
📐 **版式：三大创新点卡片 + 成果/局限/规划三栏**

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  创新点一     │  │  创新点二     │  │  创新点三     │
│              │  │              │  │              │
│ 双通道执行    │  │ 配置驱动+     │  │ 全链路合规    │
│ 架构（核心）  │  │ 确定性优先    │  │ 可审计        │
│──────────────│  │──────────────│  │──────────────│
│ 同一套5Agent │  │ workflow_     │  │ 5个独立Agent │
│ 决策大脑，    │  │ config.json  │  │ 节点各自留痕  │
│ 服务内部API  │  │ 单一事实来源  │  │ trace可追溯  │
│ 直连+外部    │  │ 规则先行、    │  │ 系统不可自动  │
│ PageAgent    │  │ LLM只填空缺， │  │ 关单，闭环    │
│ 代填两条路径  │  │ 渐进式固化    │  │ 必须人工确认  │
│              │  │              │  │              │
│ 业务约束倒逼  │  │ 兼顾准确性、  │  │ 符合金融风控  │
│ 的架构决策    │  │ 成本与可维护  │  │ 要求          │
└──────────────┘  └──────────────┘  └──────────────┘

───────────────────────────────────────────────────────

项目成果：
✅ 处理时效 24小时+ → 3–10秒
✅ 内外系统统一入口
✅ 全链路留痕

当前局限（主动说明）：
⚠️ 基于录音文件处理，暂不支持实时通话流式输入
⚠️ PageAgent依赖浏览器环境，不适用于纯API或桌面客户端系统

技术规划：
→ 实时通话流式处理，工单在通话中增量填写
→ A2A协议标准化接入，实现跨条线Agent通信
→ Agent权限与安全控制层，统一管理自动化边界
→ 建立评测基准量化各Agent表现，引入RAG对接行内知识库
```

- 三创新点卡片横排，卡头深红底白字
- 下方三栏：成果用绿色对勾，局限用橙色警告图标，规划用蓝色箭头
- 分割线1px灰色虚线，区分上下两部分

> 🎤 话术：三个创新点，核心是双通道架构——不是为了炫技，是业务约束倒逼出来的。当前有两个局限主动说明，技术规划里最重要的是实时流式处理和量化评测基准。

---

## P10 · 个人总结与展望
📐 **版式：左右分栏（左：收获4条 / 右：未来决心3条）**

```
┌─────────────────────────────┬───────────────────────────┐
│  项目收获                    │  未来决心                  │
│                             │                           │
│  ① 第一次完整经历真实业务    │  • 补齐局限（实时流式、    │
│     需求的闭环               │    量化评测、成本分析），  │
│     从需求分析、技术选型、    │    达到生产级标准          │
│     架构设计到落地实现        │                           │
│     学会在约束下做权衡        │  • 持续跟进Agent编排、     │
│                             │    多模态、A2A协议等       │
│  ② 建立系统思维——先抓最重要  │    前沿方向，保持技术      │
│     的20%                    │    敏感度                  │
│     确定性提取+双通道架构+   │                           │
│     风控门控，核心先立住      │  • 把"业务约束倒逼技术     │
│                             │    选型"这套思路带到       │
│  ③ 从"写代码"到"交付系统"    │    后续工作中——技术是     │
│     学会配置管理、数据持久化  │    工具，解决问题才是      │
│     前后端联调、SSE流式输出   │    目的                   │
│     体会到文档、注释、commit │                           │
│     message的价值            │                           │
│                             │                           │
│  ④ 技术之外的成长            │                           │
│     主动沟通需求边界          │                           │
│     快速验证MVP、卡点不钻    │                           │
│     牛角尖                   │                           │
└─────────────────────────────┴───────────────────────────┘
```

- 左栏收获4条，用序号圆圈（深红底白字）标注
- 右栏决心3条，用圆形项目符号
- 左右分栏用细虚线分隔
- 字号适中，留白充足，避免拥挤

> 🎤 话术：这次项目最大的收获不是写了多少代码，而是第一次完整经历了从需求到交付的全流程，学会了系统思维——先抓最重要的20%让骨架立起来。未来会补齐当前的局限，持续跟进前沿方向，把"业务约束倒逼技术选型"这套思路带到后续工作中。

---

## P11 · 结尾页
📐 **版式：全版深红背景，居中对齐，极简**

```
┌────────────────────────────────────────────┐
│                                            │
│                                            │
│        恳请各位领导老师批评指正              │
│                                            │
│             SunPilot                       │
│    基于多Agent的信用卡智能工单处理引擎        │
│                                            │
│              何云辉  ·  2026年7月            │
│                                            │
└────────────────────────────────────────────┘
```

- 全白字，居中
- "恳请各位领导老师批评指正"字号最大（40pt），其余小字
- 底部可加微弱的Agent流程图装饰线（同P01，保持视觉呼应）

---

## 版式多样性清单

| 页码 | 版式类型 | 核心元素 |
|------|---------|---------|
| P01 | 上下三段式 | 装饰带+主标题+信息栏 |
| P02 | （删除/口播） | 不单独成页 |
| P03 | （删除） | 不单独成页 |
| P04 | 顶部大数字+对应表 | 3大数字+痛点→方案表 |
| P05 | 三行卡片 | 三个选型决策卡片 |
| P06 | 纵向五层堆叠 | 五层架构图 |
| P07 | 纵向流程图 | 8步+双通道对比 |
| P08 | 大视频区+标签 | 演示录屏+亮点列表 |
| P09 | 三卡片+三栏 | 创新点+成果/局限/规划 |
| P10 | 左右分栏 | 收获4条+决心3条 |
| P11 | 全版色背景居中 | 结尾致谢 |

---

## 图表建议清单

| 页码 | 需要的图 | 类型 | 说明 |
|------|---------|------|------|
| P01 | 5-Agent流程细线图 | 装饰性流程图 | 半透明白色，横向，装饰用 |
| P06 | 五层架构图 | 系统架构图 | 纵向堆叠，每层不同底色，双通道特别标注 |
| P07 | 优惠券补发流程图 | 业务流程图 | 8步纵向流程+双通道对比框 |

---

## 色彩规范

- **主色（深红）**：`#C62B3B` — 标题、强调数字、图标、边框
- **辅色（深灰）**：`#2D2D2D` — 正文、说明文字
- **背景色**：`#FAFAFA` — 页面底色
- **浅色系（五层架构用）**：浅蓝/浅绿/浅黄/浅橙/浅紫 — 区分层次
- **功能色**：绿色✅（成果）、橙色⚠️（局限）、蓝色→（规划）

---

## 字体规范

- **标题**：思源黑体 Bold，48-64pt
- **副标题**：思源黑体 Bold，24-36pt
- **正文**：思源黑体 Regular，14-16pt
- **小字说明**：思源黑体 Regular，10-12pt
- **数字强调**：思源黑体 Bold，60pt+（如P04的330万+）

---

## 备注

- 所有流程图、架构图用 draw.io / ProcessOn / Figma 绘制，导出PNG嵌入PPT
- 演示视频（P08）提前录制好，控制在1-1.5分钟以内
- 每页留白充足，避免信息过载
- 红白色系贯穿全文，保持视觉一致性
