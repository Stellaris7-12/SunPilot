# 信用卡坐席智能工单系统 — 启动与使用指南

> 版本：1.1.0 | 更新日期：2026-07-19

---

## 一、环境要求

| 依赖 | 版本要求 | 说明 |
|------|---------|------|
| Python | ≥ 3.10 | |
| uv | ≥ 0.9 | Python 包管理器，[安装指南](https://docs.astral.sh/uv/) |
| Node.js | ≥ 18 | |
| npm | ≥ 9 | |

---

## 二、快速启动

> 推荐方式：**只使用项目根目录 `.venv`**，并由 `uv` 统一管理 Python 依赖。  

### 1. 进入项目目录（PowerShell）

```powershell
cd C:\Users\heyunhui\PyProjects\TicketAgent
```

### 2. 初始化 Python 环境

首次执行：

```powershell
uv sync
```

后续如果只是启动项目，不需要手动激活 `.venv`。所有后端命令都用 `uv run` 执行。

如果需要检查后端依赖是否可用：

```powershell
uv run python -c "import fastapi, openai, aiosqlite; print('backend deps ok')"
```

### 3. 配置 LLM API Key

PowerShell 当前终端临时配置：

```powershell
$env:DEEPSEEK_API_KEY="sk-your-deepseek-api-key"
```

可选配置（有默认值，按需设置）：

```powershell
$env:LLM_BASE_URL="https://api.deepseek.com/v1"
$env:LLM_MODEL="deepseek-chat"
$env:LLM_TIMEOUT="30"
$env:LLM_MAX_TOKENS="2000"
$env:LLM_TEMPERATURE="0.1"
$env:PORT="8000"
```

### 4. 启动后端

从项目根目录执行：

```powershell
uv run python -m uvicorn main:app --app-dir ai-engine --reload --port 8000
```

启动成功后输出：
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Starting Credit Card Multi-Agent System...
INFO:     Database initialized. 5 agents registered.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 5. 安装前端依赖并启动

新开一个终端：

```powershell
cd C:\Users\heyunhui\PyProjects\TicketAgent\frontend
# 初次需安装npm
npm.cmd install
# 后续可直接运行
npm.cmd run dev
```

> PowerShell 里直接执行 `npm run dev` 可能触发 `npm.ps1` 执行策略报错，所以推荐使用 `npm.cmd`。

浏览器打开 **http://localhost:5173**，系统会进入坐席工作池；也可以直接访问 **http://localhost:5173/tickets**。

### 6. Bash / Git Bash 等价命令

如果使用 Bash / Git Bash：

```bash
cd /c/Users/heyunhui/PyProjects/TicketAgent
uv sync
export DEEPSEEK_API_KEY="sk-your-deepseek-api-key"
uv run python -m uvicorn main:app --app-dir ai-engine --reload --port 8000
```

前端：

```bash
cd frontend
npm install
npm run dev
```

---

## 三、关闭服务

### 方法一：Ctrl+C（正常关闭）

在运行前后端的终端窗口中，按 **`Ctrl + C`** 即可停止该服务。

> **Windows CMD 注意**：如果 Ctrl+C 无响应（通常因为存在活跃的 SSE 长连接），按 **`Ctrl + Break`**（Pause 键），或使用方法二强制终止。

### 方法二：强制终止端口（Ctrl+C 无效时）

```bash
# 查看占用 8000 端口的进程 PID
netstat -ano | findstr :8000

# 记下输出最后一列的 PID，强制终止（替换为实际 PID）
taskkill /PID 12345 /F

# 或一步到位
for /f "tokens=5" %a in ('netstat -ano ^| findstr :8000 ^| findstr LISTENING') do taskkill /PID %a /F
```

### 恢复初始状态

如果想重置所有工单到初始状态，需要删除本地 SQLite 运行库文件并重启后端。该操作会清空本地处理记录，执行前请先确认。

PowerShell 命令如下：

```powershell
Remove-Item .\ai-engine\data\tickets.db -ErrorAction SilentlyContinue
Remove-Item .\ai-engine\data\tickets.db-shm -ErrorAction SilentlyContinue
Remove-Item .\ai-engine\data\tickets.db-wal -ErrorAction SilentlyContinue
```

### 关于虚拟环境

使用推荐的 `uv run ...` 启动方式时，不需要手动激活或停用虚拟环境。`uv` 会自动使用项目根目录 `.venv`。

---

## 四、使用流程

### 坐席工作台概览

模块 G 完成后，前端默认按一线坐席/业务员的日常操作组织，不再把 Agent Trace 放在主视觉位置。

```
┌──────────────┬────────────────────────────────────────────────────┐
│ 坐席工作池    │ 主工作区                                             │
│              │                                                    │
│ 当前待接管    │ ┌────────────────────────────────────────────────┐ │
│ 已结案        │ │ 坐席工作池 / 案件处理台                         │ │
│              │ └────────────────────────────────────────────────┘ │
│ 状态筛选      │                                                    │
│ - 全部        │ 首页：状态池、优先队列、评测摘要                    │
│ - 待处理      │                                                    │
│ - 待补充      │ 详情：案件卷宗、客户诉求、业务处理链、工具与证据      │
│ - 待确认      │                                                    │
│ - 待复核      │ 右侧：坐席动作栏、回单复核、结案入口                 │
│ - 已升级      │                                                    │
│              │ 底部：技术审计 / Agent 执行明细（默认折叠）          │
└──────────────┴────────────────────────────────────────────────────┘
```

### 1. 工作池接单

1. 打开 **http://localhost:5173/tickets**。
2. 左侧按状态查看工单池：全部、待处理、待补充、待确认、待复核、已升级、已结案。
3. 使用“场景族”筛选权益/优惠券、申请进度、资料变更、交易/争议、人工协办等类别。
4. 中间“当前优先队列”展示需要坐席关注的工单，包含工单号、标题、场景、风险、建议动作和当前状态。
5. 点击某一张工单，进入案件处理台。

### 2. 案件处理台

进入工单详情后，优先看首屏“案件卷宗”：

- **业务场景**：如权益/优惠券、资料变更、交易/争议。
- **当前状态**：待处理、待补充、待确认、待复核、已升级、已结案。
- **处理结论**：用业务话术说明系统处理结果或拦截原因。
- **下一负责人**：客户补充、坐席/复核岗、人工团队或系统继续处理。
- **证据编号**：工具执行后生成的业务证据编号。
- **建议动作**：启动 AI 辅助、请求客户补充、人工确认执行、复核回单、人工团队接管等。

中间主区域按坐席阅读顺序展示：

1. 客户诉求原文。
2. 业务处理链：接单提取、业务分诊、风险拦截、执行处理、回单生成。
3. 处理记录：业务场景、处理路径、风险判断、关键字段、工具与证据。
4. 回单与复核闭环：客户标准回单、内部通知、人工复核摘要、结案建议、回访预留。
5. 技术审计 / Agent 执行明细：默认折叠，用于调试和答辩追问。

右侧“坐席动作栏”提供当前工单的可执行动作：

- 启动 AI 辅助 / 重新生成建议。
- 填入回单草稿。
- 查看风险/缺失字段。
- 查看工具目录。
- 进入回单复核。
- 确认结案。

### 3. 评测摘要

首页底部展示评测摘要，读取 `GET /api/evaluation/metrics`：

- 状态匹配率：工程含义是“状态/预期结果匹配率”，不是实际客户结案率。
- 工具命中率：Resolution 工具选择与参数结果。
- 字段完整率：Intake 关键字段抽取结果。
- 平均耗时：真实链路平均处理耗时。
- 样本数：当前评测样本数量，模块 F 完成后为 40 条。

### Demo 场景 1：优惠券补发（低风险，工具执行型）

1. 在工作池中选择 **“活动达标未收到餐饮优惠券”**。
2. 案件卷宗显示场景为“权益/优惠券”，坐席关注点为达标原因、补发结果和证据编号。
3. 点击右侧 **“启动 AI 辅助”**。
4. 业务处理链依次推进：接单提取 → 业务分诊 → 风险拦截 → 执行处理 → 回单生成。
5. 工具与证据区域展示 `coupon.reissue`、业务结果、证据编号和下一步建议。
6. 点击 **“填入回单草稿”**，检查客户标准回单。
7. 确认无误后点击 **“确认结案”**。

### Demo 场景 2：地址修改（中风险，需人工确认）

1. 在工作池中选择 **“客户要求修改账单寄送地址”**。
2. 案件卷宗显示场景为“资料变更”，风险为中风险。
3. 点击 **“启动 AI 辅助”**。
4. 系统识别身份核验、地址字段和敏感操作要求，流程进入待确认。
5. 弹出 **“人工确认敏感操作”** 对话框。
6. 坐席检查字段和客户身份后，点击 **“确认执行”**；如不认可，点击 **“转人工处理”**。
7. 处理完成后复核回单并结案。

### Demo 场景 3：交易争议/盗刷疑似（高风险，自动升级）

1. 在工作池中选择 **“客户反馈199元交易非本人消费”** 或类似交易争议工单。
2. 案件卷宗显示场景为“交易/争议”，风险为高风险。
3. 点击 **“启动 AI 辅助”**。
4. 系统保留客户诉求、风险判断和人工复核摘要，不直接给出自动业务结论。
5. 页面显示升级原因、接管建议和后续人工归属。
6. 坐席可展开“技术审计 / Agent 执行明细”查看底层执行链路。

---

## 五、API 接口文档

后端运行后访问 **http://localhost:8000/docs** 查看 Swagger 交互式文档。

### 完整端点列表

| Method | Path | 说明 |
|--------|------|------|
| `GET` | `/api/tickets` | 获取所有工单 |
| `GET` | `/api/tickets/{id}` | 获取单个工单详情 |
| `POST` | `/api/tickets/{id}/ai-process` | 触发AI处理（同步返回） |
| `GET` | `/api/tickets/{id}/ai-process-stream` | 触发AI处理（SSE流式） |
| `POST` | `/api/tickets/{id}/confirm-action` | 人工确认工具执行 |
| `POST` | `/api/tickets/{id}/close` | 人工终审结单 |
| `GET` | `/api/tickets/{id}/trace` | 获取历史Trace |
| `GET` | `/api/tickets/{id}/ai-result` | 获取最新AI结果 |
| `GET` | `/api/agent-cards` | Agent能力卡列表 |
| `GET` | `/api/tools` | 工具注册表 |
| `POST` | `/api/tools/{tool_name}/execute` | 直接执行工具（调试用） |
| `GET` | `/api/evaluation/metrics` | 评测指标 |

### curl 测试示例

```bash
# 获取工单列表
curl http://localhost:8000/api/tickets | python -m json.tool

# 获取工单详情
curl http://localhost:8000/api/tickets/coupon | python -m json.tool

# 同步AI处理
curl -X POST http://localhost:8000/api/tickets/coupon/ai-process | python -m json.tool

# SSE流式AI处理（实时Trace）
curl -N http://localhost:8000/api/tickets/coupon/ai-process-stream

# 直接执行工具
curl -X POST http://localhost:8000/api/tools/coupon.reissue/execute \
  -H "Content-Type: application/json" \
  -d '{"customerId":"C10001","couponType":"DINING_100_20","reason":"测试补发"}'

# 结单
curl -X POST http://localhost:8000/api/tickets/coupon/close \
  -H "Content-Type: application/json" \
  -d '{"ticket_id":"coupon","final_reply":"已处理完成"}'

# Agent能力卡
curl http://localhost:8000/api/agent-cards | python -m json.tool

# 评测指标
curl http://localhost:8000/api/evaluation/metrics | python -m json.tool
```

---

## 六、项目结构

```
credit-card-agent-system/
│
├── ai-engine/                          # Python FastAPI 后端
│   ├── main.py                         # 应用入口，API/SSE/评测入口
│   │
│   ├── models/                         # Pydantic 数据模型
│   │   ├── ticket.py                   # 工单模型 + 状态枚举
│   │   ├── agent_card.py               # Agent Card（A2A-lite）
│   │   ├── agent_trace.py              # Trace 步骤模型
│   │   ├── tool_schemas.py             # 工具定义模型
│   │   ├── ai_result.py                # AI处理结果模型
│   │   ├── api_schemas.py              # API 请求/响应模型
│   │   └── database.py                 # SQLite 初始化 + 连接
│   │
│   ├── agents/                         # 5个业务Agent
│   │   ├── base.py                     # 基类 + LLM 调用
│   │   ├── classifier_agent.py         # 分类与优先级判定
│   │   ├── intake_agent.py             # 接单与信息提取
│   │   ├── escalation_agent.py         # 升级与兜底
│   │   ├── resolution_agent.py         # 解决方案与执行
│   │   ├── notification_agent.py       # 通知与回单
│   │   ├── intent_agent.py             # 旧名兼容shim
│   │   ├── extract_agent.py            # 旧名兼容shim
│   │   ├── tool_agent.py               # 旧名兼容shim
│   │   ├── verify_agent.py             # 旧名兼容shim
│   │   ├── reply_agent.py              # 旧名兼容shim
│   │   └── agent_registry.py           # Agent 注册中心
│   │
│   ├── orchestrator/                   # 编排层
│   │   ├── state_machine.py            # 工单状态机（6状态）
│   │   ├── orchestrator.py             # 核心管线（5Agent+HITL）
│   │   ├── workflow_config.py          # 轻量工作流配置加载
│   │   ├── trace.py                    # Trace 收集器
│   │   └── sse_bridge.py              # SSE 事件桥接
│   │
│   ├── tools/                          # 工具层
│   │   ├── definitions.py              # 工具定义加载
│   │   ├── registry.py                 # 工具注册中心
│   │   ├── mock_executor.py            # Mock 执行器
│   │   └── tool_router.py             # 工具 REST 路由
│   │
│   ├── data/                           # 种子数据与评测样本
│   │   ├── tickets.json                # Demo 种子工单
│   │   ├── agent_cards.json            # 5张Agent Card
│   │   ├── workflow_config.json        # 场景、必填字段、推荐工具配置
│   │   ├── tools.json                  # Mock 工具定义
│   │   └── evaluation_samples.json     # 40条评测样本
│   │
│   └── evaluation/                     # 评测模块（P1）
│       ├── evaluator.py
│       ├── run_module_f.py
│       └── smoke_module_*.py
│
├── frontend/                           # Vue 3 + TypeScript 前端
│   ├── src/
│   │   ├── types/index.ts              # TS 类型定义
│   │   ├── api/index.ts                # API 客户端
│   │   ├── router/index.ts             # 路由配置
│   │   ├── stores/ticket.ts            # Pinia 状态管理
│   │   ├── views/                      # 页面视图
│   │   │   ├── TicketListView.vue      # 坐席工作池
│   │   │   └── TicketDetailView.vue    # 案件处理台
│   │   ├── components/                 # UI 组件
│   │       ├── layout/                 # AppSidebar, AppHeader
│   │       ├── ticket/                 # 案件卷宗、客户诉求
│   │       ├── ai/                     # 业务处理链、证据、回单、审计抽屉
│   │       ├── metrics/                # 评测摘要
│   │       ├── tools/                  # 业务工具目录
│   │       └── shared/                 # StatusBadge
│   │   └── utils/business.ts           # 场景族、状态、证据和坐席动作文案
│   └── package.json
│
└── doc/                                # 项目文档
    ├── README.md                       # 文档目录索引
    ├── requirements/                   # 需求、背景、字段和修改意见
    ├── design/                         # 业务逻辑和流程设计
    ├── guides/                         # 启动与使用指南
    ├── demo/                           # Demo讲解和汇报口径
    └── planning/                       # 任务计划、发现和进度记录
```

---

## 七、架构设计要点

### 5个业务Agent协作流水线

```
工单输入 → Classifier Agent（分类与优先级判定）
         → Intake Agent（接单与信息提取）
         → Escalation Agent（升级与兜底）
         → Resolution Agent（解决方案与执行）
         → Notification Agent（通知与回单）
         → 人工终审 → 结单
```

### 三级风险路由

| 风险等级 | 处理方式 | 示例场景 |
|----------|---------|---------|
| **LOW** | 全自动5Agent管线 → 人工终审 | 优惠券补发 |
| **MEDIUM** | 升级与兜底判断后暂停 → 人工确认 → 继续执行 → 人工终审 | 地址修改 |
| **HIGH** | 跳过自动执行和自动回复 → 直接升级人工处理 | 交易争议 |

### A2A-lite Agent Card

每个Agent通过 `agent_cards.json` 自描述能力、输入输出Schema、依赖关系和运行约束。编排器在启动时加载所有卡片，按拓扑排序决定执行顺序。

### SSE流式Trace

前端通过 `EventSource` 连接 `/api/tickets/{id}/ai-process-stream`，实时接收每个Agent的执行状态：
- `agent_start` → Agent开始工作
- `agent_thinking` → Agent进度消息  
- `agent_complete` → Agent完成（含结果+耗时）
- `workflow_paused` → 管线暂停等待人工确认
- `workflow_complete` → 管线完成（含完整结果）

---

## 八、常见问题

**Q: 启动后端报 `AuthenticationError: 401`？**

A: 未配置有效的 LLM API Key。请按第二章设置 `DEEPSEEK_API_KEY` 环境变量。支持 DeepSeek 和 Qwen（OpenAI 兼容接口）。

**Q: 前端页面空白？**

A: 确认后端已启动在 `localhost:8000`。当前前端 API 客户端默认访问 `http://localhost:8000/api`，所以端口或后端地址变更时需要同步修改 `frontend/src/api/index.ts`。

**Q: 工作池里看不到某个状态的工单？**

A: 左侧状态筛选和“场景族”筛选会共同生效。先切回“全部”和“全部场景”；如果仍没有，检查后端 `GET /api/tickets` 返回的工单状态。

**Q: 为什么技术 Agent 名称不在主界面展示？**

A: 模块 G 已改为坐席业务工作台。主界面只展示接单提取、业务分诊、风险拦截、执行处理、回单生成等业务动作；底层 `IntakeAgent`、`ClassifierAgent` 等执行明细保留在“技术审计 / Agent 执行明细”折叠区。

**Q: 首页的“状态匹配率”是不是客户真实结案率？**

A: 不是。`closedLoopSuccessRate` 当前工程含义是“状态/预期结果匹配率”，用于评测 Agent 输出是否匹配标注预期，不代表真实客户闭环结案率。

**Q: 如何重置工单状态？**

A: 删除 `ai-engine/data/tickets.db`、`ai-engine/data/tickets.db-shm`、`ai-engine/data/tickets.db-wal` 后重启后端，系统会重新创建数据库并加载种子数据。删除前请确认你不需要保留本地处理记录。

**Q: 如何切换 LLM 模型？**

A: 设置 `LLM_MODEL` 环境变量，例如 PowerShell 中执行 `$env:LLM_MODEL="qwen-plus"`。

**Q: 如何添加新的工单场景？**

A: 1) 在 `data/tickets.json` 中添加工单数据；2) 在 `data/workflow_config.json` 中增加场景、必填字段、推荐工具和通知模板；3) 在 `agents/classifier_agent.py` 的 Prompt 中添加新的分类类型；4) 如需新工具，在 `data/tools.json` 中添加工具定义。

---

## 九、Demo 录制建议

汇报演示时建议按以下顺序：

1. **启动展示**（10秒）：展示两个终端窗口分别启动后端和前端。
2. **坐席工作池**（20秒）：展示状态池、场景族筛选、当前优先队列和评测摘要，说明这是一线坐席工作台。
3. **场景1 - 优惠券补发**（60秒）：展示案件卷宗、业务处理链、工具与证据、回单复核、确认结案。
4. **场景2 - 地址修改**（45秒）：展示资料变更中风险、人工确认敏感操作、继续执行、回单复核。
5. **场景3 - 交易争议/盗刷疑似**（30秒）：展示高风险升级、人工接管原因、禁止自动结论。
6. **技术审计追问**（15秒）：展开“技术审计 / Agent 执行明细”，展示底层 Agent Trace 和耗时。
7. **架构亮点**（30秒）：Agent Card / 状态机 / SSE 流式事件 / Tool Registry / 证据链 / 评测指标。
